kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: whoami
# No default CNI and no kube-proxy: Cilium provides both.
networking:
  disableDefaultCNI: true
  kubeProxyMode: none
nodes:
  - role: control-plane
  - role: worker
  - role: worker
